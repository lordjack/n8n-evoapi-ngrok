# Dockerfile com ENTRYPOINT explícito
FROM n8nio/n8n:latest

# Configurações essenciais
ENV NODE_ENV=production
ENV N8N_BASIC_AUTH_ACTIVE=true

# Expor porta
EXPOSE 5678

# Usar ENTRYPOINT explícito
ENTRYPOINT ["docker-entrypoint.sh"]
CMD ["n8n"]
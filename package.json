{
  "name": "n8n-evoapi-ngrok",
  "version": "1.0.0",
  "description": "N8N + Evolution API + ngrok integration",
  "main": "index.js",
  "scripts": {
    "dev": "docker-compose --env-file .env up -d",
    "dev:down": "docker-compose down",
    "dev:logs": "docker-compose logs -f",
    "prod": "docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d",
    "prod:down": "docker-compose -f docker-compose.prod.yml down",
    "prod:logs": "docker-compose -f docker-compose.prod.yml logs -f",
    "prod:clean": "docker-compose -f docker-compose.prod.yml down -v"
  },
  "keywords": [
    "n8n",
    "evolution-api",
    "whatsapp",
    "automation"
  ],
  "author": "lordjack",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
